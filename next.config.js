/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['placeholder.com'], // Add other image domains as needed
  },
  // Add any other Next.js configuration options here
}

module.exports = nextConfig
